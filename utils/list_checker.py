def is_in_list(input_str:str, reference_list:list):
    """
    checks if the input string is contained in the reference list

    if so returns True, otherwise returns False
    """
    if input_str in reference_list:
        return True
    else: 
        return False
